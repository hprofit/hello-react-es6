{
  "author": "scott preston",
  "name": "hello-react-es6",
  "description": "hello world of react using es6 vs. es5",
  "version": "0.0.1",
  "dependencies": {
    "http-server": "^0.9.0",
    "jquery": "^3.0.0",
    "react": "^15.1.0",
    "react-dom": "^15.1.0"
  },
  "scripts": {
    "build": "babel src -d lib",
    "prestart" : "npm run build",
    "start" : "http-server -p 5000"
  },
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-react": "^6.5.0"
  }
}
